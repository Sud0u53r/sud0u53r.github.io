name: Test Artifacts

on:
  workflow_dispatch:

jobs:
  test-artifact:
    runs-on: ubuntu-latest
    steps:
      - name: Test Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-artifact
          path: .
